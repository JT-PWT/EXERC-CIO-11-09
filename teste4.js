let carro = "hatch";

switch (carro) {
    case "hatch":
        console.log("Compra efetuada com sucesso.");
        document.write("Compra efetuada com sucesso.")
        break;
    case "sedan":
        console.log("Tem cesteza que não prefere este modelo?");
        document.write("Tem cesteza que não prefere este modelo?")
        break;
    case "motocicletas":
        console.log("Tem cesteza que não prefere este modelo?")
        document.write("Tem cesteza que não prefere este modelo?")
        break;
    case "caminhonetes":
        console.log("Tem cesteza que não prefere este modelo?")
        document.write("Tem cesteza que não prefere este modelo?")
        break;

    default:
        console.log("Não trabalhamos com este tipo de automóvel aqui.")
        document.write("Não trabalhamos com este tipo de automóvel aqui.")


}